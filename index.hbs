<header>
  <h1>{{ @site.title }}</h1>
  <h2>{{ @site.description }}</h2>
</header>

<main role="main">

  <!-- For each one of the posts, display the title -->
  {{#foreach posts}}
    <h1><a href="{{url}}">{{title}}</a></h1>
  {{/foreach}}

</main>

<!-- Pagination block -->
{{pagination}}